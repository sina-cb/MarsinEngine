package Engine.Forces;

/**
 * Created by IntelliJ IDEA.
 * User: Sina Solaimanpour
 * Date: 10/18/11
 * Time: 10:47 PM
 */
public class Weight extends Force {

    /**
     * Default Constructor, It Sets The X Force 0 so that weight get simulated properly and It will set The
     * Y Force 10 just for fun
     */
    public Weight() {
        setForceX(0);
        setForceY(10);
    }

    /**
     * It Sets The X Force 0 so that weight get simulated properly and It will set The
     * Y Force to parameter passed by user
     *
     * @param weight For The Y Force Value
     */
    public Weight(double weight) {
        setForceX(0);
        setForceY(weight);
    }

}
